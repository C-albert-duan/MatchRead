# Module: Tokens (`packages/tokens`)

**Package:** `@matchread/tokens`  
**Role:** Shared design tokens (color, type, spacing) for the MatchRead visual system.

## Surface

- Entry: `packages/tokens/src/index.ts`
- Consumed by `apps/web` styles/components

## Boundaries

- Presentation constants only — no business rules.
- Prefer tokens over one-off hex/spacing in new UI so brand stays consistent.

## Related

- Consumer: [web-app.md](./web-app.md)
