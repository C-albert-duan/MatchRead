# MatchRead — Interaction Specification

**US Open 2026 web launch.** Every interactive element: what it does, where it goes, and how it behaves when things are missing, slow or broken.

> Generated from `js/registry.js` by `tools/build-docs.js`. Do not edit this file by
> hand — edit the registry and regenerate, or the prototype and the document will
> disagree within a month.

---

82 interactive elements across 29 screens.

---

## Entry

### Landing page  
`/` · Launch MVP

#### Start a league (prominent)

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Signed out: routes to /sign-in with ?next=/leagues/new. Signed in: routes straight to /leagues/new. |
| Destination | /sign-in?next=%2Fleagues%2Fnew or /leagues/new |
| Required data | Session presence only. |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | Navigation. The `next` round-trip is the single most-broken thing in a magic-link flow and is verified in runbook 10 step 6. |
| Failure state | A malformed `next` falls back to / rather than erroring — safeNext() rejects by shape, never by blocklist. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### See what it looks like

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Routes to the live component showcase. |
| Destination | /showcase |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Calendar row

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Opens the public tournament page. |
| Destination | /tournaments/[ref] |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | When no tournaments are imported the strip is not rendered at all — an empty calendar is not a design state, it is a missing import. |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### Sign in  
`/sign-in` · Launch MVP

#### Email field

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Type |
| Resulting behaviour | Validated on submit, not on keystroke — validating an email while it is being typed marks every address invalid for as long as it takes to write one. |
| Destination | — |
| Required data | Local state only until submit. |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | Field gets aria-invalid, the message is rendered below it and referenced by aria-describedby, and focus moves to the field. |
| Animation / transition | None. Never animate a validation error into view. |

#### Send me a link (prominent)

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click / Enter |
| Resulting behaviour | Calls supabase.auth.signInWithOtp with emailRedirectTo pointing at /auth/callback, carrying the sanitised `next`. |
| Destination | Same route, "check your email" state. |
| Required data | The email. The `next` parameter, already passed through safeNext(). |
| Disabled conditions | Disabled while a request is in flight, and for 30 seconds after a successful send, so a second tap cannot burn a rate-limit slot. |
| Loading behaviour | Label becomes "Sending". The button keeps its width so the row does not reflow. |
| Empty state | — |
| Success state | The form is replaced by the confirmation, which states the address it was sent to — a mistyped address is the commonest failure and the screen must make it visible. |
| Failure state | Rate limit gets its own message naming the wait, because "something went wrong" sends the user to support for a problem that resolves itself. Supabase built-in email is limited to a handful an hour and is NOT beta-grade; a transactional provider is item 7 on the founder checklist. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Change language

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Select |
| Resulting behaviour | Sets the locale cookie and re-renders server-side. |
| Destination | Same route |
| Required data | preferences.locale (0026) when signed in; cookie only when not. |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### Check your email  
`/sign-in (submitted state)` · Launch MVP

#### Send it again

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Re-issues the OTP for the same address. |
| Destination | — |
| Required data | — |
| Disabled conditions | Disabled for 30 seconds after each send, with the remaining time named in the label rather than hidden in a tooltip. |
| Loading behaviour | Label becomes "Sending". |
| Empty state | — |
| Success state | A status region confirms the resend. The screen does not change shape. |
| Failure state | Rate-limit message names the wait. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Use a different address

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Returns to the form with the field pre-filled and focused. |
| Destination | /sign-in |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### Lightweight onboarding  
`— not built` · Post Launch

#### Display name

`Needs Engineering`

| Aspect | Specification |
|---|---|
| User action | Type + save |
| Resulting behaviour | Writes profiles.display_name. This is the name that appears in every standings row in every league, and it is the only identity decision the product asks anyone to make. |
| Destination | — |
| Required data | — |
| Disabled conditions | Save disabled until the field is non-empty. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | A uniqueness collision is not possible — display names are not unique. Only length is validated. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Time zone

`Needs Engineering`

| Aspect | Specification |
|---|---|
| User action | Select |
| Resulting behaviour | Defaults to Intl.DateTimeFormat().resolvedOptions().timeZone and writes preferences.time_zone. |
| Destination | — |
| Required data | preferences.time_zone (0020) |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | If the browser reports nothing, the field is required rather than defaulted. There is no default zone anywhere in this product; a default is how a member in California reads a New York lock time and misses the tournament by fourteen hours. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Skip

`Needs Engineering`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Proceeds. The member appears in standings under the local part of their email until they set a name. |
| Destination | /leagues |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

## Leagues

### My leagues (home dashboard)  
`/leagues` · Launch MVP

#### League row

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Opens the league home. |
| Destination | /leagues/[slug] |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | Border colour line → line-strong over 120ms. No lift, no shadow: elevation in this product is ground, not depth. |

#### Start a league (prominent)

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Opens the creation form. |
| Destination | /leagues/new |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Empty state action

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Same destination as above; the empty state names what happens next rather than saying "no data". |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | "No leagues yet — Start one for a single tournament, or for a whole season. Invite whoever you would text about a five-setter." |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### Start a league  
`/leagues/new` · Launch MVP

#### League name

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Type |
| Resulting behaviour | Validated on submit against the Zod schema in lib/validation/league.ts. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | Empty → "Give your league a name". Over 60 characters → "Keep the name under 60 characters". Both are validation CODES returned by the Server Action and translated at the surface — an action that returned an English sentence would be a locale decision made on the server for a page rendered in another language. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Format — single tournament / season

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Select one |
| Resulting behaviour | Chooses whether the league dies with its tournament or keeps its people across the year. This is the product's central claim and it is presented as two consequences, not as a dropdown. |
| Destination | — |
| Required data | — |
| Disabled conditions | Immutable after creation. The form says so at the point of choice, not in a confirmation afterwards. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | "Choose a single tournament or a full season" |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Visibility — private / public

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Select one |
| Resulting behaviour | Private is the default. Public exposure is an explicit, opt-in projection rather than a loosened policy. |
| Destination | — |
| Required data | — |
| Disabled conditions | Changeable later by the commissioner. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | "Choose private or public" |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Tournament picker

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Select |
| Resulting behaviour | Shown only when format = single. Lists tournaments whose draw has not locked. |
| Destination | — |
| Required data | — |
| Disabled conditions | Hidden entirely for a season league rather than disabled — a disabled control that can never apply is furniture. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | If no tournaments are imported: the picker is replaced by a line saying the calendar is being confirmed, and a season league is still creatable. A commissioner must never be blocked by an import. |
| Success state | — |
| Failure state | "Choose the tournament this league plays" |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Create league (prominent)

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Server Action create_league: inserts the league, adds the caller as commissioner, mints an invite token, generates the slug. |
| Destination | /leagues/[slug] |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Label becomes "Creating". The form is not disabled wholesale — only the submit — so a failed attempt leaves every field editable. |
| Empty state | — |
| Success state | Redirect to the new league, invite panel expanded, focus on the copy control. The commissioner's next act is always sharing. |
| Failure state | Field errors render inline AND as a summary above the form ("There is 1 problem with this form."), with focus moved to the summary. A form that reports errors only inline is unusable when the failing field is below the fold. |
| Animation / transition | None. A form that animates on submit is a form that feels slower than it is. |

---

### Invite friends  
`/leagues/[slug] (panel)` · Launch MVP

#### Copy link

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | navigator.clipboard.writeText on the absolute invite URL. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | Label becomes "Copied" for 2.2 seconds, then returns. The label is the feedback — no toast, because the control is already where the eye is. |
| Failure state | The clipboard can be denied: insecure context, permissions policy, an embedded webview. On rejection the product says "Could not copy. Select the link above and copy it by hand." and the link stays selectable. A control that does nothing and reports nothing is indistinguishable from a broken one, and the previous implementation caught the rejection and said nothing at all. |
| Animation / transition | Label crossfade only, 120ms. |

#### The link itself

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Select / long-press |
| Resulting behaviour | Rendered as selectable monospaced text, never as an image or a truncated span with a hidden full value. |
| Destination | — |
| Required data | Same token. |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Revoke and re-issue

`Needs Engineering`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Mints a new token and invalidates the old one. |
| Destination | — |
| Required data | — |
| Disabled conditions | Commissioner only. For a member the control is absent, not disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | The displayed link changes in place and a status region announces it. |
| Failure state | On failure the OLD link stays displayed — showing a new token that was not persisted would hand out a link that does not work. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### Join a league  
`/join/[token]` · Launch MVP

#### Join this league (prominent)

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Signed in: Server Action accept_invite, inserting a league_members row and enrolling the member in the field of every tournament the league already has. Signed out: routes to /sign-in carrying ?next=/join/[token]. |
| Destination | /leagues/[slug] |
| Required data | The token. The caller's user id, server-side only. |
| Disabled conditions | Disabled while joining. Absent when already a member — that case shows "Go to the league" instead, because a Join button that means Open is a lie. |
| Loading behaviour | Label becomes "Joining". |
| Empty state | — |
| Success state | Redirect to the league home. The Daily Check on arrival is the draw-pending one: your league is ready, invite your friends. |
| Failure state | An invalid or revoked token renders a page that names what happened and offers the landing page — never a 404, because the user did nothing wrong and a 404 reads as their mistake. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Sign in first

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | The `next` round trip. This is the exact case routes.ts safeNext() exists to make safe: carrying a destination through an auth redirect is also the textbook open-redirect vulnerability. |
| Destination | /sign-in?next=%2Fjoin%2F[token] |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | An absolute URL, a protocol-relative URL, a backslash-smuggled variant or anything with a scheme is refused and falls back to home rather than erroring. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### League home  
`/leagues/[slug]` · Launch MVP

#### The check action

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | One action, chosen by the check's own kind. Draw pending → copy the invite. Awaiting entries → open the bracket. Finished → see the result. The check decides; the component never picks its own destination. |
| Destination | Varies by check kind |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | A check with no action renders no button. An always-present call to action would make every day look identical, which is the failure this screen exists to avoid. |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Standings preview

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Read / click "All 12" |
| Resulting behaviour | Five rows. A league home that renders forty is a leaderboard. |
| Destination | /leagues/[slug]/t/[ref] |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Tournament row

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Opens that tournament inside this league. |
| Destination | /leagues/[slug]/t/[ref] |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Copy invite

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | See the invite screen. Present for the commissioner in every state, and prominent in draw-pending. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### Season standings  
`/leagues/[slug]/season` · Launch MVP

#### How points work

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Expand |
| Resulting behaviour | Discloses the scoring rule in one paragraph: every tournament is worth the same maximum whatever its size, so a perfect bracket at a 250 is worth exactly what a perfect bracket at a Slam is. Points are awarded when an event finishes, not while it is being played. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | Height transition, 220ms, ease-standard. Bypassed under prefers-reduced-motion by the global rule. |

#### Tournament row

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Opens that event. |
| Destination | /leagues/[slug]/t/[ref] |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### Between tournaments  
`/leagues/[slug] (state)` · Launch MVP

#### Add the next tournament

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Commissioner only. Adds an event to the league; every member joins its field automatically. |
| Destination | — |
| Required data | — |
| Disabled conditions | Absent for a member, not disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | "Nothing on the calendar yet. Add the next tournament and everyone in the league gets it the moment the draw lands." |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Countdown

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Read |
| Resulting behaviour | Renders the lock in the VIEWER's zone, with the venue clock as a footnote when the two differ. |
| Destination | — |
| Required data | preferences.time_zone (0020) · tournaments.venue_time_zone |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | If the next draw has no announced date: "The next draw has not been announced. Your league is waiting, not finished." Never a blank countdown. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

## Tournament

### Tournament in a league  
`/leagues/[slug]/t/[ref]` · Launch MVP

#### Open / Review / View my bracket

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | One control, three labels, chosen by state: "Open my bracket" before entry, "Review my bracket" once saved, "View my bracket" after lock. An action keeps the same destination through the flow; only the promise changes. |
| Destination | /leagues/[slug]/t/[ref]/bracket |
| Required data | — |
| Disabled conditions | Never disabled. After the lock it opens read-only, which is a different thing from being unavailable. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Submit my bracket

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Server Action submit_entry. Separate from saving: a saved bracket is a private draft; submitting makes it an entry in THIS league. That separation is what lets the league see who has committed without seeing what anyone picked. |
| Destination | — |
| Required data | — |
| Disabled conditions | Disabled after the lock instant, enforced by a Postgres trigger and not by the button. The client is never the source of truth. |
| Loading behaviour | Label becomes "Submitting". |
| Empty state | — |
| Success state | The standings row for the member flips from "Not yet" to "In". The picks stay invisible to everyone. |
| Failure state | A lock that has passed returns a refusal code the surface translates. The button never guesses the lock has passed from the client clock. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Withdraw my entry

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Quiet tone, never prominent. Removes the entry; the bracket itself is kept. |
| Destination | — |
| Required data | — |
| Disabled conditions | Disabled after lock. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | Status flips back to "Not yet". |
| Failure state | Refusal code, translated. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Standings table

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Read |
| Resulting behaviour | Before the lock it shows who has committed and NOTHING about what they picked. After the lock it shows score, movement and whether each member's champion is still alive. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | "Nobody is in this field yet." |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Settlement disclosure

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Read |
| Resulting behaviour | Renders only when the standings are not current, with wording per state — "we are still counting" and "the last attempt failed" ask different things of the reader. Sourced from draw_settlement_health so the interface reports what the database knows rather than guessing from a timestamp. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None. role=status, not role=alert — information to receive, not an interruption. |

---

### Bracket — entry  
`/leagues/[slug]/t/[ref]/bracket` · Launch MVP

#### A name in a slot

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click / Space / Enter / arrow keys |
| Resulting behaviour | Picks that player to win the match, and advances them into the next round's slot. Picking the other name un-picks this one. The pair is a radiogroup of two radios, not two toggle buttons — a mutually exclusive choice is what a radiogroup is, and the previous implementation announced "Alcaraz, toggle button, pressed" with nothing to say the two were alternatives of the same match. |
| Destination | — |
| Required data | Local state, then autosaved. |
| Disabled conditions | Not rendered as a button at all after the lock — it becomes a span. A disabled button that will never enable is furniture. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | The chosen name takes WEIGHT AND A LEFT RULE, never a fill. A filled row reads as a result; a rule reads as "this one continues", which is what a pick is. It is set in ink, not green — choosing is a claim, not a result. |
| Failure state | Downstream slots the pick invalidates are cleared, and the clearing is visible rather than silent. |
| Animation / transition | Background colour on hover only, 120ms. The advance is NOT animated: a pick that flies across the draw is a pick you cannot make sixty-three more of. |

#### Autosave

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Automatic |
| Resulting behaviour | Every change schedules a save 1.2 seconds later; a further change reschedules it. submit_bracket is a whole-bracket replace and is idempotent, so a retry on a bad connection cannot corrupt anything and the last write wins by construction. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | A status region reads "Saving your bracket" — text, not a spinner. |
| Empty state | — |
| Success state | "Bracket saved". Then it settles back to "Changes save automatically". |
| Failure state | "Your bracket did not save. Nothing has been lost — try again." The wording matters: the member has spent twenty minutes and needs to know the work is still in the page. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Progress

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Read |
| Resulting behaviour | "n of 127 picks made". A count, not a percentage — a draw has a number of matches and that number is meaningful. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | At zero it reads "Pick your way through to a champion." |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### The scroll region

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Tab / arrow keys / drag |
| Resulting behaviour | A focusable, named region. A 128 draw is wider than any viewport and reaching the sixth round must not require a trackpad. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | Native scrolling. Round headings stay sticky beneath the app header so a column is never anonymous. |

#### Submit my bracket (prominent)

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Turns the private draft into an entry in this league. |
| Destination | — |
| Required data | — |
| Disabled conditions | Disabled until every match has a pick, AND after the lock. The incomplete case names what is missing rather than greying out silently. |
| Loading behaviour | "Submitting". |
| Empty state | — |
| Success state | A confirmation state on the same screen — not a new page. The member sees their own bracket with the commitment recorded above it. |
| Failure state | Refusal code translated at the surface. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### Bracket — locked and settled  
`/leagues/[slug]/t/[ref]/bracket` · Launch MVP

#### A settled name

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Read |
| Resulting behaviour | Correct picks turn Tournament Green with weight and a green left rule. Incorrect picks turn red and are struck through. Every state ALSO carries a visually hidden word — correct, incorrect, still alive, not yet played — because state carried only by colour is a WCAG 1.4.1 failure and silent to a screen reader. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | Colour transition 220ms on first paint after settlement. This is the one place in the product where motion is emotional rather than explanatory, and it is still 220ms. |

#### A voided pick

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Read |
| Resulting behaviour | Void outranks every other state, including a settled result — a player can win round one and withdraw before round two, and in that case the later slot is void while the earlier one is not. The slot gets a dashed border, a dashed strike-through, NO colour, and the explanation inside the slot rather than in a legend elsewhere. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | Void is neither a verified fact nor a miss. Spending green or red on it would say something untrue. |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Champion

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Read |
| Resulting behaviour | "Your champion: [name]" above the draw. The single fact that decides whether a bracket still has a future. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### Result artifact  
`/leagues/[slug]/t/[ref]/result` · Launch MVP

#### Copy share text

`Partial`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Copies the artifact sentence — "2nd of 12 in Fourth Floor Slam Challenge at US Open — 588 points. MatchRead." |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | Label becomes "Copied". |
| Failure state | Same clipboard-denied path as the invite: name the failure and leave the text selectable. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Download image

`Needs Engineering`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Renders the artifact to a shareable image. |
| Destination | — |
| Required data | — |
| Disabled conditions | Does not exist on web. apps/mobile has ShareCanvas and shareImage.ts; there is no web equivalent. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Not-yet-available state

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Read |
| Resulting behaviour | "A result page appears once a tournament has been scored and the league frozen. If this was your league, check back after the final." — an explanation, not a 404. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | Same copy. |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

## Public

### Tournaments  
`/tournaments` · Launch MVP

#### Tournament row

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Opens the public tournament page. The 3px court hairline is the only colour on the row and it means surface, nothing else. |
| Destination | /tournaments/[ref] |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### Players  
`/players` · Launch MVP

#### Player row

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Opens the player page. |
| Destination | /players/[ref] |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | "Rankings appear once the first tournament of the season has been imported." |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

## System

### Global navigation (app shell)  
`— wraps every route` · Launch MVP

#### Skip to content

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Tab from the top of the page, then Enter |
| Resulting behaviour | The first focusable element on every page. Invisible until focused, then it appears against the canvas and moves focus to <main id="main">. It is not decoration: with nine header controls, a keyboard user reaching page content without it costs nine tabs on every navigation. |
| Destination | #main on the current page |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | Appears instantly on focus. No transition — a skip link that animates in is a skip link that is slower than tabbing past it. |

#### Wordmark

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Returns to the landing page. The "Tennis brackets" descriptor beside it is an eyebrow, not a link, and is not part of the hit target. |
| Destination | / |
| Required data | None. |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Nav link (Tournaments · Players · Leagues)

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Standard navigation. The active link carries aria-current="page" and is distinguished by weight, never by colour — a green nav link would claim the item is a verified result. Leagues is absent entirely when signed out rather than shown disabled: a control that exists but refuses is worse than a control that does not exist. |
| Destination | /tournaments · /players · /leagues |
| Required data | The session, for the Leagues link only. |
| Disabled conditions | Never disabled. Leagues is omitted when there is no session. |
| Loading behaviour | Route-level loading.tsx renders the page skeleton; the header itself never enters a loading state and never moves. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Language control (EN)

`Partial`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Opens language selection. Deliberately placed before the session control: a user who has landed in a language they cannot read must be able to reach this without first solving the sign-in screen in that language. |
| Destination | Settings (in-shell); the page re-renders server-side in the chosen locale. |
| Required data | preferences.locale (0026) when signed in; a cookie when not. |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | Every string on the page changes at once. Nothing shifts position — the three locales are laid out to the same measure, and ja is the one that tests it. |
| Failure state | The write fails silently to the cookie and the UI still changes. A language the user cannot read is not the place to surface a database error. |
| Animation / transition | None. The locale change is a server render, not a transition. |

#### Sign in / Sign out

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Signed out, this is the standard action linking to /sign-in with the current path as next, guarded by safeNext(). Signed in, it is a quiet action that ends the session. |
| Destination | /sign-in?next=… · / after sign out |
| Required data | The session. |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | Sign out returns to the landing page signed out. No confirmation dialog — signing back in is one email away, and a modal here would be friction pretending to be care. |
| Failure state | If sign-out fails the session is cleared client-side regardless and the user lands on /. Never leave someone who asked to leave still signed in. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Footer

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Read |
| Resulting behaviour | Two sentences, both permanent. "Free to play. No entry fees, no wagering." states what this product is, on every page, including the ones a stranger lands on first. The second disclaims affiliation with tournament organisers, which is what makes descriptive use of tournament names defensible. |
| Destination | — |
| Required data | None. Neither sentence is translated from a variable — both are i18n keys like everything else. |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None ever. |

---

### Settings  
`— in the shell` · Launch MVP

#### Language

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Select |
| Resulting behaviour | en / es / ja. 427 keys, all three complete, build-gated on completeness. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | The page re-renders in the new language server-side. No flash of the old language, because nothing is translated on the client. |
| Failure state | A locale that is not supported falls back to en rather than erroring. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Time zone

`Needs Engineering`

| Aspect | Specification |
|---|---|
| User action | Select |
| Resulting behaviour | Every instant in the product is rendered in this zone, with the venue clock as a footnote when they differ. |
| Destination | — |
| Required data | — |
| Disabled conditions | No UI exists for this today — it is read from preferences or the browser. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Sign out

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Clears the session and returns to the landing page. |
| Destination | / |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### Notifications  
`— not connected` · Post Launch

#### Everything on this screen

`Future Feature`

| Aspect | Specification |
|---|---|
| User action | — |
| Resulting behaviour | Nothing here is wired. The Daily Check is a PULL habit by choice: the product's bet is that "I wonder what happened in my league today" is a stronger reason to return than a push telling you. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### Error pages  
`/404 · error boundary` · Launch MVP

#### Try again

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Calls the boundary reset. Present on the error page and ABSENT on not-found, because retrying a URL that does not exist is not a recovery. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Error reference

`Partial`

| Aspect | Specification |
|---|---|
| User action | Read / copy |
| Resulting behaviour | A short id the member can quote. It is the only thing that makes a support message actionable today, because there is no error tracking anywhere in the product. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Go to MatchRead

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | On the global error boundary this is a raw anchor rather than a router Link — the one place in the app where a hand-written anchor is correct, because the router may itself be broken. |
| Destination | / |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### System states  
`— cross-cutting` · Launch MVP

#### Loading

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Automatic |
| Resulting behaviour | A skeleton of the page's own shape, never a spinner. A spinner says "wait"; a skeleton says "this is what is arriving", which is the calmer claim and the honest one. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | A 1.4s sheen, neutralised under prefers-reduced-motion by the global rule in globals.css. |

#### Empty

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Automatic |
| Resulting behaviour | Names what is missing and what happens next. Never "no data". Every empty state in this product is an invitation to act. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Offline

`Needs Engineering`

| Aspect | Specification |
|---|---|
| User action | Automatic |
| Resulting behaviour | A status banner stating that this is the last data MatchRead had, and that bracket edits are queued. The bracket stays editable offline — losing twenty minutes of entry to a dropped connection at 10:47 on lock day is the failure this design exists to prevent. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Provider error

`Needs Engineering`

| Aspect | Specification |
|---|---|
| User action | Automatic |
| Resulting behaviour | Live scores are not arriving; results shown are from the last successful REST reconciliation sweep. The banner names the sweep rather than saying "something went wrong", because the data on screen is real and merely late. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### Component showcase  
`/showcase` · Launch MVP

#### Every component

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Read |
| Resulting behaviour | Rendered in its real states. This is the drift guard: it fails the build rather than rotting silently, which is the normal fate of design artefacts. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

## Operator

### Operations dashboard  
`/founder` · Launch MVP

#### A metric tile

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Read |
| Resulting behaviour | Every coloured rule is accompanied by a STATE WORD. The colour is aria-hidden. Colour was the only carrier of health on the page whose entire job is to say whether the platform is broken — the same defect the bracket had, unfixed here because nobody audits an operations surface. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Runbook

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | Opens the incident runbook for that signal. |
| Destination | docs/runbooks/* |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Replay badge

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Read |
| Resulting behaviour | States plainly when a rehearsal tournament is in play, because figures that mix replay and production data are worse than no figures. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### Draw changes (operator)  
`/founder/disruption` · Launch MVP

#### Replacement search

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Type |
| Resulting behaviour | Searches players NOT already in this draw. The word "lucky loser" is deliberately absent from the label: the seat can be taken by a lucky loser, a qualifier, an alternate or a special exempt, and naming one would make the other three look wrong. |
| Destination | — |
| Required data | — |
| Disabled conditions | Under two characters it says "Type at least two letters" rather than searching. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | "No player outside this draw matches that name." |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Reason

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Type |
| Resulting behaviour | Required, and recorded permanently against the change. "Write what you would want to read in six months." |
| Destination | — |
| Required data | — |
| Disabled conditions | Preview is blocked until it is long enough. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | "A few more words — this is the audit trail." |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Preview this change

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click |
| Resulting behaviour | THE MOST IMPORTANT SCREEN IN THE OPERATOR FLOW. It states the product consequence, never the mechanism: "four members lose a pick", not "four rows will be updated". One of three remedy paragraphs is shown — notify (brackets still editable), void (locked and never played), or eliminate (already competed). |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | The preview renders. Nothing has been written. |
| Failure state | Refusal codes are translated into sentences. incoming_already_in_draw is not something anybody should read; "That player already holds another seat in this draw. A player cannot appear twice — brackets built on a duplicated name cannot be scored." is. |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Apply this change

`Already Exists`

| Aspect | Specification |
|---|---|
| User action | Click → confirm |
| Resulting behaviour | Two steps, always. The confirm names the blast radius in members and picks. |
| Destination | — |
| Required data | — |
| Disabled conditions | Disabled until a preview has been taken. Absent entirely on a completed draw, where a replacement would change nothing. |
| Loading behaviour | "Applying". |
| Empty state | — |
| Success state | A verification summary: leagues affected, brackets adjusted, picks voided, whether members will be told, and whether standings are queued for rescoring. |
| Failure state | Nothing is applied. The refusal names which check failed. |
| Animation / transition | None anywhere in this flow. An irreversible action should never feel fast. |

---

## Daily picks (not on web)

### Today's matches  
`— mobile only` · Visual Exploration

#### Match card

`Future Feature`

| Aspect | Specification |
|---|---|
| User action | Tap |
| Resulting behaviour | Opens the match detail. |
| Destination | /match/[id] |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Date strip

`Future Feature`

| Aspect | Specification |
|---|---|
| User action | Swipe |
| Resulting behaviour | Moves between days. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### Match detail  
`— mobile only` · Visual Exploration

#### A baseline

`Future Feature`

| Aspect | Specification |
|---|---|
| User action | Tap |
| Resulting behaviour | Picks that player. The baseline lights. |
| Destination | — |
| Required data | — |
| Disabled conditions | Locked at first serve, enforced by a Postgres trigger. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### The community split

`Future Feature`

| Aspect | Specification |
|---|---|
| User action | Read |
| Resulting behaviour | HIDDEN until the viewer has picked this match. Enforced in RLS and in the RPC, not just in the UI. It prevents herd bias and keeps the product a scouting habit rather than a poll — one of the three product decisions enforced in Postgres rather than in the interface. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

#### Comments

`Future Feature`

| Aspect | Specification |
|---|---|
| User action | Read / write |
| Resulting behaviour | Open only AFTER the match finishes, and only inside that match. Anti-toxicity as a database constraint. No global feed, no DMs. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### Prediction history / Match IQ  
`— mobile only` · Visual Exploration

#### Rating

`Future Feature`

| Aspect | Specification |
|---|---|
| User action | Read |
| Resulting behaviour | Scores DIFFICULTY, not volume: correct → +K·(1−p), incorrect → −K·p, where p is the engine probability of the picked side. Expected value at the model's own price is exactly zero, so no strategy farms rating. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

### User profile  
`— not on web` · Post Launch

#### Everything

`Future Feature`

| Aspect | Specification |
|---|---|
| User action | — |
| Resulting behaviour | Not built on web, and not on the launch path. /players is a page about TENNIS PLAYERS, not about members — do not conflate the two routes. |
| Destination | — |
| Required data | — |
| Disabled conditions | Never disabled. |
| Loading behaviour | Not applicable — navigation only. |
| Empty state | — |
| Success state | — |
| Failure state | — |
| Animation / transition | None beyond the standard hover: colour only, 120ms, ease-standard. |

---

