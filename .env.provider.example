# Copy to `.env.provider` (gitignored). Used only by provider scripts / future Railway worker.
# Never put these on Vercel. Never commit real values.

RAPIDAPI_KEY=<your-rapidapi-key>
RAPIDAPI_HOST=tennis-api-atp-wta-itf.p.rapidapi.com

# Required for live reconcile (not needed for --dry-run)
# MATCHREAD_INGEST_URL=https://<project-ref>.supabase.co/functions/v1/ingest-events
# INGEST_SECRET=<same-as-supabase-secrets>
